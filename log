add framework of d_nf ok, pass compile.

change d_pkt_counter.cc to d_pkt_counter.cu change the structure of makefile to support compiling .cu file. pass compile. 

add pkt queue class, pass compile.

pkt_counter and flowmonitor port ok, pass compile.

httpparser port ok, pass compile.!!!!